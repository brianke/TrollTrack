<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrollTrack.Features.Dashboard.DashboardView"
             xmlns:f="clr-namespace:TrollTrack.Features.Dashboard"
             xmlns:m="clr-namespace:TrollTrack.Features.Shared.Models">

    <RefreshView IsRefreshing="{Binding IsRefreshing}"
                 Command="{Binding RefreshDashboardCommand}" >

        <ScrollView>
            <Grid Padding="15,0,15,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Text="{Binding RefreshStatus}" 
                       FontSize="12" 
                       TextColor="DarkBlue"
                       HorizontalOptions="Center"
                       Margin="0,0,0,5"
                       Grid.Row="0"/>

                <!-- Current Location -->
                <Border BackgroundColor="LightBlue" 
                        Stroke="DodgerBlue"
                        StrokeThickness="2"
                        StrokeShape="RoundRectangle 12"
                        Margin="0,0,0,15"
                        Padding="15"
                        Grid.Row="1">
                    <StackLayout>
                        <HorizontalStackLayout HorizontalOptions="Center">
                            <Label Text="📍 " 
                                   FontSize="18" 
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center" />
                            <Label Text="{Binding LocationName}" 
                                   FontSize="18" 
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center" />
                        </HorizontalStackLayout>

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width=".15*" />
                                <ColumnDefinition Width=".25*" />
                                <ColumnDefinition Width=".15*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Latitude -->
                            <Label Text="Latitude:" 
                                   FontAttributes="Bold" 
                                   VerticalOptions="Center"
                                   Grid.Row="0" Grid.Column="0" />
                            <Label Text="{Binding FormattedLatitude}" 
                                   FontFamily="Courier" 
                                   FontSize="16"
                                   Margin="10,0,0,0"
                                   Grid.Row="0" Grid.Column="1" />
                            <!-- Decimal degrees (smaller text) -->
                            <Label Text="{Binding CurrentLatitude, StringFormat='{0:F6}'}" 
                                   VerticalTextAlignment="Center"
                                   FontSize="13" 
                                   TextColor="Gray"
                                   Margin="10,0,0,0"
                                   Grid.Row="0" Grid.Column="2" />

                            <!-- Longitude -->
                            <Label Text="Longitude:" 
                                   FontAttributes="Bold" 
                                   VerticalOptions="Center" 
                                   Grid.Row="1" Grid.Column="0" />
                            <Label Text="{Binding FormattedLongitude}" 
                                   FontFamily="Courier" 
                                   FontSize="16"
                                   Margin="10,0,0,0"
                                   Grid.Row="1" Grid.Column="1" />
                            <!-- Decimal degrees (smaller text) -->
                            <Label Text="{Binding CurrentLongitude, StringFormat='{0:F6}'}" 
                                   VerticalTextAlignment="Center"
                                   FontSize="13" 
                                   TextColor="Gray"
                                   Margin="10,0,0,0" 
                                   Grid.Row="1" Grid.Column="2" />

                        </Grid>

                    </StackLayout>
                </Border>

                <!-- Weather Information -->
                <Border BackgroundColor="LightBlue" 
                        Stroke="DodgerBlue"
                        StrokeThickness="2"
                        StrokeShape="RoundRectangle 12"
                        Margin="0,0,0,15"
                        Padding="10"
                        Grid.Row="2">
                    <StackLayout>
                        <Label Text="🌤️ Current Weather Conditions"
                               FontSize="18" 
                               FontAttributes="Bold"
                               HorizontalOptions="Center" />
                        <Grid Margin="5" x:DataType="m:WeatherData">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width=".4*" />
                                <ColumnDefinition Width=".4*" />
                                <ColumnDefinition Width=".4*" />
                            </Grid.ColumnDefinitions>

                            <StackLayout Grid.Column="0" >
                                <Label Text="{Binding WeatherEntity.Temperature, StringFormat='{0:F0}°'}"
                                       FontSize="36"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center" />
                                <Label Text="{Binding WeatherEntity.WeatherCondition, StringFormat='☁️ {0}'}"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                                <HorizontalStackLayout HorizontalOptions="Center" Margin="0,5,0,0">
                                    <Label Text="{Binding WeatherEntity.WindSpeed,StringFormat='💨 {0:F0} mph'}" 
                                           FontAttributes="Bold"/>
                                    <Label Text="  " />
                                    <Label Text="➤" Rotation="{Binding WeatherEntity.WindDirection}" />
                                </HorizontalStackLayout>

                            </StackLayout>
                            <StackLayout Grid.Column="1">
                                <Image Source="sunrise.png" 
                                       MaximumHeightRequest="70"
                                       Margin="0,0,0,10"/>
                                <Label Text="{Binding WeatherEntity.Sunrise, StringFormat='{0:HH:mm tt}'}" 
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                            </StackLayout>

                            <StackLayout Grid.Column="2">
                                <Image Source="sunset.png" 
                                       MaximumHeightRequest="78"
                                       Margin="0,2,0,0"/>
                                <Label Text="{Binding WeatherEntity.Sunset, StringFormat='{0:HH:mm tt}'}" 
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                            </StackLayout>
                        </Grid>

                    </StackLayout>
                </Border>
            </Grid>
        </ScrollView>
    </RefreshView>
</ContentPage>